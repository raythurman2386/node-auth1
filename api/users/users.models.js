const bcrypt = require('bcryptjs');
const db = require('../../data/db-config');

// find
const find = () => {
  return db('')
}

// findBy

// add

// findById

module.exports = {
  find,
  findBy,
  add,
  findById
}